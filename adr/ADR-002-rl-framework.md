# ADR-002: RL framework — JAX (Mava) vs PyTorch (HARL) vs other

**Status.** Accepted (2026-05-13)
**Authors.** Farhad Safaei
**Reviewers.** _to fill_
**Tags.** v0.2 §13, HMARL

## Context
v0.2 §13 names this as an open question. The team's existing strength (JAX vs PyTorch) is the dominant input; HARL has a maintained PyTorch reference at PKU-MARL/HARL. The decision interacts directly with the B5 baseline selection (§3.8), the partner-zoo construction methodology (§3.5), and the benchmark physics stack already anchored to ManiSkill2's SAPIEN/PyTorch layer (ADR-001).

## Considered alternatives

| # | Option | Source / advocate | Pros | Cons |
|---|--------|-------------------|------|------|
| A | PyTorch + HARL fork | [Tier-1 #25] | Direct B5 baseline; community; matched to HARL's monotonic-improvement guarantees | Tooling overlap with VLA partners (most VLAs are PyTorch) |
| B | JAX + Mava | [external] | Performance; differentiable physics integration | Smaller MARL community; HARL re-implementation cost |
| C | TorchRL + BenchMARL | [Tier-3 #24] | Standardised benchmarking infra; HetGPPO compatibility | Less mature than HARL for heterogeneous monotonic-improvement |

## Decision
Adopt **Option A — PyTorch + HARL fork**, contingent on a team-skill audit confirming no blocking JAX expertise that would change the calculus for Option B.

## Rationale
HARL's sequential-update scheme (Lemma 4, Algorithm 1 in `notes/tier1/25_harl.md`) is the only published MARL algorithm with a provable monotonic joint-return improvement guarantee for heterogeneous-agent settings; its PyTorch implementation is actively maintained at PKU-MARL/HARL and runs directly on Bi-DexterousHands, the closest analogue to our bimanual manipulation target. Adopting the HARL fork delivers HAPPO (on-policy B5) and HATD3 (off-policy B5) as near-drop-in baselines (`stolen_ideas.md` row 13), eliminating re-implementation risk at a point where baseline credibility is more important than training throughput. The benchmark fork anchored to ManiSkill2's SAPIEN/PyTorch physics stack (ADR-001, `notes/tier2/54_maniskill2.md`) and the VLA partner checkpoints used in Phase 1 (OpenVLA `notes/tier2/32_openvla.md`, CrossFormer `notes/tier2/33_crossformer.md`) are all PyTorch-native; a single-stack environment minimises interface complexity at the action-interception boundary (v0.2 §6). Option B (JAX + Mava) is deferred unless the team-skill audit reveals a strong JAX majority, because HARL re-implementation cost is uncompensated by differentiable-physics gains at Phase-1 scale. Option C (TorchRL + BenchMARL) is complementary, not competitive: BenchMARL's standardised logging (`notes/tier3/refs.bib #24`) can be adopted as a reporting layer over the HARL training loop without replacing it.

## Evidence basis (links to reading notes)
- [notes/tier1/25_harl.md] — HAPPO and HATD3 as B5 on/off-policy baselines; multi-agent advantage decomposition lemma (Lemma 4) as theoretical foundation; empirical proof that MAPPO fails completely on 17-agent Humanoid while HAPPO succeeds (§5.2, Fig. 8); AHT adaptation requires frozen-partner ego-only wrapper (open question, §45 note)
- [notes/tier1/23_hetgppo.md] — per-agent separate-parameter design; behavioral-typing brittleness under deployment noise (§6, Figs. 4 & 6); DTDE GNN pattern as architecture candidate; HetGPPO as B3 candidate requiring AHT fork validation
- [notes/tier3/refs.bib #24 BenchMARL] — community standard for reproducible MARL benchmarking; complementary reporting layer candidate

## Consequences
- **Project scope**: ego-agent training loop is PyTorch throughout Phase 1; HAPPO is the B5 on-policy baseline and HATD3 is the B5 off-policy baseline; full JAX/Mava migration is out of Phase-1 scope.
- **v0.2 plan sections affected**: §3.8 (HAPPO/HATD3 confirmed as B5 baseline pair), §13 (RL framework resolved), §3.4 (per-agent separate parameters locked in — supported by HetGPPO behavioral-typing evidence), §3.5 (HARL-trained frozen policies as learning-based zoo candidates require AHT wrapper validation before admission).
- **Other ADRs**: depends on ADR-001 (ManiSkill2 PyTorch anchor); informs ADR-009 (partner-zoo construction — HARL-trained policies as zoo candidates); BenchMARL logging is a non-ADR implementation decision.
- **Compute / time / hiring**: IsaacGym GPU simulation required for Bi-DexHands B5 validation; HARL frozen-partner AHT wrapper estimated 1–2 weeks of engineering before B5 runs can begin.

## Risks and mitigations
- [**AHT frozen-partner HAPPO breaks the monotonic-improvement guarantee**] Theorem 7 in `notes/tier1/25_harl.md` requires all agents to update jointly; freezing the partner's policy eliminates the formal guarantee. Mitigation: prototype ego-only HAPPO with a frozen partner on a 2-agent MPE task and verify empirically that per-epoch reward shows a **positive learning slope at α = 0.05** — the canonical *learning-signal trip-wire* implemented in `concerto.training.learning_signal_check.check_positive_learning_slope`, a one-sided slope test against the null "slope ≤ 0" on the per-episode reward curve. The statistic is a *trip-wire*, **not** a formal monotonic-improvement guarantee — the "empirical-guarantee" phrasing the pre-rename module used was overclaimed and was renamed on 2026-05-16 (external-review P1-1; see Revision history). The check distinguishes four outcomes via `concerto.training.learning_signal_check.CheckStatus`: PASSED (slope strictly positive, p < α), FAILED (flat or regressing), INSUFFICIENT_DATA (curve too short to evaluate — replaces the pre-amendment vacuous-pass branch flagged by external-review P1-2), and INVALID (curve contains NaN / inf). The trainer-side construction gate is `chamber.benchmarks.ego_ppo_trainer._assert_partner_is_frozen` (ADR-009 §Consequences; plan/05 §6 #3), which refuses to build `EgoPPOTrainer` against a partner with any `requires_grad=True` parameter unless the partner is wrapped behind the `chamber.partners.interface.PartnerBase` shield.
- [**PyTorch version conflicts between HARL and VLA partner inference**] OpenVLA and CrossFormer may pin different PyTorch/CUDA versions than the HARL training loop, causing silent incompatibilities at the action-interception boundary. Mitigation: containerise the HARL trainer and each VLA inference server in separate Docker images communicating over a thin action-bus interface; the conformal CBF layer (v0.2 §6) is the natural inter-process boundary and isolates version drift.

## Reversibility
**Type-2** (two-way door). Porting HAPPO/HATD3 to JAX/Mava is non-trivial (estimated 2–4 person-weeks) but technically feasible at any point before Phase-2 scale-up; no data artifacts or trained checkpoints are framework-locked. The exit ramp is available as long as HARL customisations have not diverged substantially; the practical reversal deadline is Phase-1 mid before team-specific JAX expertise erodes.

## Validation criteria
By Phase-1 mid: ego-only HAPPO (frozen partner) reproduces published HARL Bi-DexterousHands ShadowHandOver score within 5 percentage points of Table 2 in `notes/tier1/25_harl.md`; HATD3 matches within 5pp on ShadowHandCatchOver2Underarm. Both runs must use per-agent separate parameters and the AHT frozen-partner wrapper.

## Open questions deferred to a later ADR
- (#verify, from `notes/tier1/25_harl.md`) Can the ego-only HAPPO frozen-partner adaptation preserve a weaker convergence guarantee, or does it reduce to an empirical baseline only? If the guarantee is entirely lost, document the implication for §3.8 B5 claim language.
- (#design-decision, from `notes/tier1/25_harl.md`) Should HATD3 or HAPPO be prioritised for the bimanual manipulation target? HATD3 is more sample-efficient but replay-buffer complicates AHT adaptation; defer to prototype benchmark results.
- (#verify, from `notes/tier1/23_hetgppo.md`) Is HetGPPO (B3) feasible as a frozen-partner AHT baseline with the VMAS + RLlib stack? Confirm before committing to the §3.8 baseline set.

## Revision history

- 2026-05-21 amendment (Surface 6 remediation; P1.05.8 closes PR #182's Tier-2 acceptance gap): the regression pin landed on 2026-05-21 (entry below) flipped from xfail to xpass under this slice and the `@pytest.mark.xfail(strict=True)` marker is removed in the same PR (precedent: xfail-strict-lifecycle hygiene — when an xfail-strict test xpasses, the marker MUST go in the same commit as the fix, so the regression net stays a positive assertion rather than a silently-passing xpass). The Surface 6 remediation widens `chamber.envs.stage1_obs_filter.Stage1ASStateSynthesizer` so the ego uid's synthesised `state` Box concatenates `[ego_qpos, ego_qvel, partner_qpos, partner_qvel, cube_pose, goal_pos, tcp_pose]` (per-condition dim env-emit-dependent — pinned per-condition by the new Tier-1 + Tier-2 tests). The trainer's `_flat_ego_obs` and `EgoPPOTrainer.from_config` are unchanged; the wrapper's contract is "synthesise the trainer's required `state` key", and the wrapper is now the place that defines what `state` *means* for the env (cube / goal / TCP / partner all flow through). The non-ego (partner) entry's `state` Box stays at `concat(qpos, qvel)` of the partner alone — only the ego sees the task. OM-condition pass-through is preserved verbatim (P1.05.6 / issue #177 owns the OM vision-head remediation; `Stage1ASStateSynthesizer` short-circuits on `is_om_condition=True`). Surface 1 (the rubber-stamp success rule `mean_reward > -0.30 or terminated`) is dropped in the same slice: `chamber.benchmarks.stage1_as._run_one_episode` and `chamber.benchmarks.stage1_om._run_one_episode` now compute `success = bool(terminated) and not bool(truncated)`, matching the env's own `evaluate()` predicate (`is_obj_placed & is_robot_static`); the `_SUCCESS_THRESHOLD` constants are deleted from both adapters. Stage-1a's MPE stand-in does not emit `terminated=True`, so Stage-1a success rates become structurally zero — correct per ADR-007 §Stage 1a (Stage 1a is rig-validation, not the ≥20 pp gate). Cross-ref: ADR-007 §Revision history Rev 12; ADR-009 §Decision direct-observation-of-partner-pose paragraph (also amended in this PR). No change to the §Decision content. Status stays **Accepted (2026-05-13)**.
- 2026-05-21 amendment (Tier-2 acceptance gap; PR #182 follow-up): The Stage-1b AS launch (P1.05 PR 5b, PR #182) surfaced a Tier-2 acceptance gap. The P1.04 Tier-2 tests (`tests/integration/test_trained_policy_factory_real.py`) verified that `EgoPPOTrainer` ran end-to-end against a real `Stage1PickPlaceEnv` instance but did NOT verify what the trainer actually read from the env's obs dict. Surface 6 of the failure investigation (`spikes/results/stage1-failure-investigation/2026-05-20/INVESTIGATION.md` + `surface_6_obs_contract_audit.txt`) identified that `chamber.benchmarks.ego_ppo_trainer._flat_ego_obs` indexes only `obs["agent"][ego_uid]["state"]` — a flat 18-D `concat(qpos, qvel)` synthesised by `Stage1ASStateSynthesizer` from the ego robot's joint state alone. Cube pose, goal position, ego TCP pose, and partner qpos+qvel are present in the env's obs dict (under `obs["extra"]` and the partner's sibling subtree) but live in paths the trainer never reads; the ego trained for 100k frames blind to task-relevant signal. **Tier-2 acceptance contract extension.** For any trainer ↔ env wire, the Tier-2 acceptance suite MUST include an assertion about what task-relevant fields the trainer actually reads from the env's obs — not merely "the trainer runs end-to-end." A regression pin landed at `tests/integration/test_trainer_obs_reader_contract.py` and is marked `pytest.mark.xfail(strict=True)` pending the Surface 6 remediation slice. The pin is Tier-1 (composes `tests.fakes.FakeStage1PickPlaceObs` with the real `Stage1ASStateSynthesizer` and constructs `EgoPPOTrainer` via `from_config`) so the regression net fires on every `make verify`, not only on the GPU box where the original gap shipped from. The assertion is a structural proxy on `trainer._obs_dim` (today 18; required ≥30 after remediation) rather than a per-field offset introspection — the consultation brief at `spikes/results/stage1-failure-investigation/2026-05-20/CONSULTATION_BRIEF.md` owns the concat-order decision; the pin is intentionally robust to whichever layout the remediation lands. When the remediation widens the trainer's view, the xfail flips to xpass and the marker MUST be removed in the same PR (Stage-2 Task S2-D in the remediation prompt). No change to the §Decision content. Status stays **Accepted (2026-05-13)**.
- 2026-05-20 amendment (cu128 wheel-index pin; cuda-major coupling discipline; PR closes the OSV-Scanner follow-up scheduled in PR #180): `[tool.uv.sources]` now routes `torch` through PyTorch's cu128 wheel index (`download.pytorch.org/whl/cu128`) on Linux via a `marker = "sys_platform == 'linux'"` guard; Mac / Windows hosts continue to resolve torch from the PyPI default. The `torch` version specifier in `[project].dependencies` widens from `>=2.8,<2.9` to `>=2.9,<3.0` and `uv lock` resolves to `torch 2.11.0+cu128` on Linux (`torch 2.12.0` on other platforms, where CUDA is irrelevant). **Why this slice.** The P1.05 OSV-Scanner refresh on 2026-05-20 surfaced PYSEC-2025-203/204/206 (torch availability-only advisories, fix-in-2.9.0). The first bump attempt (PR #180 review thread) widened the version specifier to `<3.0` and `uv lock` produced `torch 2.12.0+cu130` — incompatible with the project's RTX 2080 dev box on NVIDIA driver 535.x, which exposes CUDA 12.x runtime only. `torch.cuda.is_available()` returned `False`. Reverted; PR #180 merged with the three advisories in the OSV-Scanner ignore list. This follow-up PR pins the wheel index at the tooling layer so future torch bumps within the cu128 series don't drag in the cu130-default-wheel breakage. Cleared from `osv-scanner.toml`: PYSEC-2025-203, -204, -206. The remaining 12 advisories (torch unpatched local-access + joblib + markdown) wait on upstream patches per `osv-scanner.toml`. **Cuda-major coupling discipline (new project rule).** Torch wheels default to the latest CUDA toolkit available at release time (torch 2.12 shipped cu130 by default). Future torch bumps MUST verify cuda-major compatibility with the project's target GPU envelope before merge: (a) reproduce on the RTX 2080 box (driver 535.x → CUDA 12.x runtime); (b) confirm `torch.cuda.is_available() == True` after `rm -rf .venv && uv sync`; (c) confirm a tensor `.cuda()` alloc + arithmetic round-trips. The `[[tool.uv.index]]` pin makes the wheel-index intent explicit in `pyproject.toml`; box-side acceptance lives in PR description per the same discipline this PR follows. Cloud-A100 hosts (the operational alternative for PR 5b) typically ship newer drivers (>= 555) supporting both cu128 and cu130; the cu128 pin is upper-compatible on those hosts. The pin can be raised to cu13x once the RTX 2080 box upgrades its driver — tracked at next box-refresh window. **Repro scripts.** `scripts/repro/stage1_{as,om}_stage1b.sh` now log the resolved `torch.__version__ + torch.version.cuda` at script start, alongside the existing `device_report()` line, so the archive's auditable provenance carries a one-line answer to "what torch produced this archive?". No change to the §Decision content. Status stays **Accepted (2026-05-13)**.
- 2026-05-19 amendment (HARL distribution-mechanism supersession; PR for #132): the 2026-05-16 `[dependency-groups].train` PEP 735 group is dissolved. HARL now ships as the `harl-aht` PyPI distribution (the CONCERTO fork at `fsafaei/harl-fork` v0.1.0, published to TestPyPI under OIDC trusted publishing — see `fsafaei/harl-fork#1`). `harl-aht>=0.1.0,<0.2.0` is now a direct entry in `[project].dependencies` of `concerto-multirobot/pyproject.toml`, replacing the `harl @ git+https://github.com/fsafaei/harl-fork.git@<SHA>` direct-reference pin that the 2026-05-16 train-group scoping had moved out of wheel METADATA. The distribution name is `harl-aht` (PyPI) and the import path is `harl` (matches upstream PKU-MARL/HARL source code), so all `from harl...` import sites are unchanged. `[tool.uv.sources]` routes `harl-aht` to TestPyPI for the v0.x line; this block goes away when the v1.x line promotes the dependency to real PyPI. **Determinism.** The SHA-256 wheel hash recorded in `uv.lock` is the immutable identifier (stronger than the prior 40-character git-SHA pin, since PyPI does not allow re-uploading the same version even by the project owner). The `v0.1.0-aht` git tag at `56436e4` remains in the fork's history as the source-code marker for the AHT patch; the PyPI release version is `0.1.0` (the `-aht` suffix is not PEP 440 compliant and would be rejected by the uploader). **Code-side consequences.** The lazy-import + custom `ModuleNotFoundError` wrapper in `chamber.benchmarks.ego_ppo_trainer.EgoPPOTrainer.__init__` and `chamber.partners.frozen_harl.FrozenHARLPartner._ensure_loaded` are collapsed — the `from harl... import ...` statements now sit at module top (HARL is guaranteed to be present once `pip install concerto-multirobot` resolves). The lazy-import contract test (`tests/unit/test_harl_lazy_import.py`) is dropped along with the wrapper it pinned. The `test_harl_fork_patches.py` recipe-structure test is unchanged — the patch recipe at `scripts/harl-fork-patches/` is still the source of the v0.1.0-aht delta and ships in CONCERTO as the reproducible build instructions for the fork. **Install-time semantics.** `pip install concerto-multirobot` (with the `--extra-index-url https://test.pypi.org/simple/` for v0.x consumers, mirroring the v0.x policy for `concerto-multirobot` itself) now pulls HARL automatically; the prior `uv sync --group train` two-step is no longer required. No change to the Decision; status stays **Accepted (2026-05-13)**.
- 2026-05-16 amendment (external-review P1-1 + P1-2): renames
  `concerto.training.empirical_guarantee` →
  `concerto.training.learning_signal_check` and the public function
  `assert_positive_learning_slope` →
  `check_positive_learning_slope`. The slope test is a *trip-wire* for
  §Risks #1, not a formal monotonic-improvement guarantee — the
  pre-rename "empirical-guarantee" label and the `assert_*` prefix
  both overclaimed the contract. The renamed function returns a
  report (does not raise) and now carries an explicit
  :class:`concerto.training.learning_signal_check.CheckStatus`
  taxon: PASSED / FAILED / INSUFFICIENT_DATA / INVALID. The pre-fix
  branch returned ``passed=True`` on curves below ``min_episodes``,
  silently masking the insufficient-data condition (P1-2); the new
  branch returns ``CheckStatus.INSUFFICIENT_DATA`` with
  ``passed=False``. NaN / inf in the reward curve return
  ``CheckStatus.INVALID`` rather than crashing inside
  ``scipy.stats.linregress``. The legacy ``passed: bool`` field on
  :class:`SlopeReport` is preserved for v0.5.x consumers (derived from
  ``status``) and emits a :class:`DeprecationWarning` on each
  construction; removal target v0.6.0. The legacy module path
  ``concerto.training.empirical_guarantee`` is preserved as a
  deprecation shim that re-exports every renamed symbol and emits a
  :class:`DeprecationWarning` on the legacy function name via
  ``__getattr__``. The trainer-side partner-freeze gate (§Risks #1
  cross-reference) is unchanged. No change to the Decision; status
  stays **Accepted (2026-05-13)**.
- 2026-05-16 amendment (HARL distribution-mechanism scoping; PR for #131): the HARL fork is scoped to a PEP 735 dependency group (`[dependency-groups].train` in `pyproject.toml`) rather than a runtime dependency. PyPI and TestPyPI reject wheel `METADATA` containing `git+URL` direct references (`400 Can't have direct dependency` from `legacy/`), and the `harl @ git+https://github.com/fsafaei/harl-fork.git@<SHA>` pin would otherwise block every upload of `concerto-multirobot`. Dependency groups are uv-only and are *not* shipped in wheel `METADATA`, so the wheel uploads cleanly while source-checkout users continue to get HARL via `uv sync --group train`. **Install-time semantics.** Source-checkout developers run `uv sync --group dev --group train` (canonical via `make install`) and get the full test surface, including `tests/unit/test_partner_frozen_harl.py`, `tests/unit/test_ego_ppo_trainer.py`, and `tests/integration/test_draft_zoo.py`. Wheel consumers (`pip install concerto-multirobot`) get the safety stack (`concerto.safety.*`), the comm stack (`chamber.comm.*`), and the evaluation harness (`chamber.evaluation.*`) — but **not** HARL; importing `chamber.benchmarks.ego_ppo_trainer` and `chamber.partners.frozen_harl` succeeds (the HARL imports are scoped to method bodies), but instantiating `EgoPPOTrainer` or calling `FrozenHARLPartner._ensure_loaded()` raises a `ModuleNotFoundError` whose message names the install command (`uv sync --group train`) and points at the Phase-1 follow-up issue. **Determinism.** Pinning is unchanged: the same 40-character commit SHA on the `fsafaei/harl-fork` git history. The lazy-import contract is pinned by `tests/unit/test_harl_lazy_import.py`, which exercises both the module-import path (no top-level `import harl`) and the runtime-error path (helpful error names install command and #132). **Supersession (Phase-1).** This scoping is a v0.x interim. The long-term answer is **Option A** in the analysis on #131: publish the HARL fork to PyPI under a distinct name (`harl-aht`) at the `v0.1.0-aht` tag, then replace the dependency-group entry with a versioned PyPI pin (`harl-aht==0.1.0`) in `[project.optional-dependencies].train` (extras, opt-in via `pip install concerto-multirobot[train]`). SHA-256 wheel-hash determinism is *stronger* than git-SHA-pin determinism (immutable upload, no force-moveable tags), and the round-trip restores a single-command training install for PyPI users. Tracked in #132.
- 2026-05-15 amendment (§Risks #1 statistic): the canonical learning-signal check is now the one-sided slope test (renamed on 2026-05-16 to `concerto.training.learning_signal_check.check_positive_learning_slope`, α = 0.05; legacy alias preserved in `concerto.training.empirical_guarantee`), not the moving-window-of-10 non-decreasing-fraction statistic the prior wording named. Issue #62's root-cause writeup motivated the redesign in PR #72: the moving-window statistic has poor signal-to-noise on per-episode rewards at the Phase-0 100k-frame budget (the window-mean stdev is comparable to the total drift the trainer produces, so the non-decreasing fraction sits near 0.5 — a random walk — even when the policy is materially improving). The slope test integrates over the whole curve and rejects the null "slope ≤ 0" with p-values astronomically below any reasonable α when learning is real. The legacy `assert_non_decreasing_window` helper is retained in the same module as a reference for the Pardo-2017-aware shape but is no longer the CI gate. The trainer-side partner-freeze gate (plan/05 §6 #3) is also surfaced in §Risks #1 — the runtime check (`chamber.benchmarks.ego_ppo_trainer._assert_partner_is_frozen`) refuses to build the trainer against any partner with a `requires_grad=True` parameter (ADR-009 §Consequences), so the empirical-guarantee assertion never runs against a non-frozen partner in the first place. See [ADR-INDEX footnote](ADR-INDEX.md#open-work-flags) for the open-work flag on this amendment.
- 2026-05-13 status re-classification: status changed from Proposed to **Provisional** under the new ADR status taxonomy (see [ADR-INDEX §Status taxonomy](ADR-INDEX.md#status-taxonomy)); no Decision content is altered.
- 2026-05-13 lock: promoted to **Accepted (2026-05-13)** under the solo-developer working policy in ADR-INDEX (M4b training stack is merged on `main`); no Decision content is altered.
- 2026-05-13 footnote (M4 partner-stack `artifacts_root` convention): `EgoAHTConfig.artifacts_root` (Pydantic field) is the canonical source of truth for training runs that own a config plane; the partner-stack adapters (`chamber.partners.frozen_mappo`, and by extension the forthcoming `frozen_harl`) are constructed via `chamber.partners.registry.load_partner` which has no access to `EgoAHTConfig`, and therefore resolve `artifacts_root` from the `CONCERTO_ARTIFACTS_ROOT` environment variable (default `./artifacts`, matching `EgoAHTConfig.artifacts_root`'s own default). No Decision content is altered; this footnote pins the asymmetry so future readers know both planes exist by design.
