# ADR-004: Safety filter formulation — exp CBF, HO-CBF, MPC, learned

**Status.** Accepted (2026-05-13) (dependency update 2026-05-08 in light of ADR-007 rev 3 staged rollout)
**Open work.** Per-step safety bound under heterogeneous action spaces and the implied safety-stack refactor; see [§Open questions deferred to a later ADR](#open-questions-deferred-to-a-later-adr) and [ADR-INDEX footnote a](ADR-INDEX.md#open-work-flags).
**Authors.** Farhad Safaei
**Reviewers.** _to fill_
**Tags.** v0.2 §6.2 + §13, SAFE

## Context
v0.2 §6.2 default position is exponential CBF in Phase 2. ADR resolves the formulation choice and its assumption envelope, citing the Tier-1 Huriot & Sibai 2025 read (note 42, mislabelled "Singh" in schedule.csv) and the Tier-2 safety cluster (notes 37, 44, 45). Foundational anchors for the formulation are Ames et al. 2017 (canonical CBF-QP safety filter, IEEE TAC) and Ames et al. 2019 (ECC tutorial); the conformal layer rests on Shafer & Vovk 2008 and Angelopoulos & Bates 2023. The core tension: exponential CBF (Option A) is mature and certifiable but requires known partner dynamics, while our black-box partner setting invalidates that assumption at deployment.

## Considered alternatives

| # | Option | Source / advocate | Pros | Cons |
|---|--------|-------------------|------|------|
| A | Exponential CBF | [Ames et al. 2017 IEEE TAC; Tier-2 #37 W-A-E; Tier-2 #38 Cheng] | Mature; tractable; certifiable | Conservative; requires known dynamics; high-relative-degree systems need work |
| B | High-order CBF | [Tier-3 #43 Kim 2026] | Handles higher-DoF coupling | Newer; less literature on manipulation |
| C | MPC safety filter | [external] | Predictive horizon useful for Tier 3 | Computational cost; harder to certify |
| D | Conformal CBF (Huriot & Sibai-style) | [Tier-1 #42 Huriot & Sibai] | Adapts restrictiveness to observed prediction error; black-box partner OK; distribution-free guarantee | Partner predictor must exist; requires online recalibration; average-loss (not per-step) guarantee |

## Decision
Exponential CBF (Option A) in Phase 2 with a conformal-adaptation overlay from Huriot & Sibai 2025 (Option D) to handle the black-box partner. The within-robot manipulation-grade filter uses the OSCBF structure from Morton & Pavone 2025 (note 44). High-order CBF is a Phase-3 research extension; MPC is reserved for Tier-3 external use only.

## Rationale
Ames, Xu, Grizzle, and Tabuada (2017) establish the canonical CBF-QP safety filter — min ‖u−û‖² subject to a forward-invariant CBF constraint — that the project's entire safety stack inherits. Ames et al. (2019, ECC) consolidates the theory; Wang, Ames, and Egerstedt (2017) (notes/tier2/37_wang_ames_egerstedt_2017.md) extend it to decentralized multi-robot collision avoidance with the proportional constraint-budget split (αi/(αi+αj) by acceleration limit) that directly handles heterogeneous embodiments in §6.2; this pattern is retained as the structural backbone because it wraps any nominal controller without accessing its internals, which is precisely the property needed when the partner policy is black-box. Garg et al. 2024 (notes/tier2/45_lindemann_safety_survey.md) confirm via Table 1 that no existing method simultaneously achieves formal safety guarantees, dynamics-free operation, and distributed deployment — establishing that Option A alone is insufficient and that the dynamics-free axis must be addressed by the conformal extension. Huriot & Sibai 2025 (notes/tier1/42_singh_2024.md) close this gap: their conformal slack variable λ (eq. 5) added additively to each CBF constraint, updated by λ_{k+1} = λ_k + η(ε − l_k), provides a distribution-free ε + o(1) long-term risk bound (Theorem 3) that holds even under adversarial partner predictions, making Option D the necessary extension of Option A rather than a competing alternative. The conformal slack variable inherits the distribution-free coverage guarantees of conformal prediction (Shafer & Vovk 2008; Angelopoulos & Bates 2023); Huriot & Sibai 2025 specialise it to a CBF online-update setting, producing the ε + o(1) long-term risk bound. Morton & Pavone 2025 (notes/tier2/44_morton_2025.md) supply the within-robot manipulator-grade filter: the OSCBF two-level QP (‖Wⱼ(q̇−q̇_nom)‖² + ‖Wₒ(ν−ν_nom)‖² with slack relaxation for conflicting constraints) operates at kilohertz rates on a 7-DOF arm and directly addresses the CBF-QP infeasibility problem flagged in Garg et al. §7.3. Options B and C are deferred because HO-CBF's manipulation literature is immature (Tier-3, no done note) and MPC's certification cost outweighs its predictive benefit at Phase-2 scope.

## Evidence basis (links to reading notes)
- Ames, Xu, Grizzle, Tabuada (2017), "Control Barrier Function Based Quadratic Programs for Safety Critical Systems," IEEE TAC — canonical CBF-QP formulation; the entire safety stack is a wrapper layer above this primitive.
- Ames et al. (2019, ECC), "Control Barrier Functions: Theory and Applications" — tutorial consolidating CBF theory used in §Rationale for the relative-degree and reciprocal-CBF framing.
- Shafer & Vovk (2008), "A Tutorial on Conformal Prediction," JMLR; Angelopoulos & Bates (2023), "Conformal Prediction: A Gentle Introduction," FnT-ML — foundational CP machinery the conformal slack overlay rests on.
- [notes/tier1/42_singh_2024.md] — conformal slack variable λ (eq. 5), update rule λ_{k+1} = λ_k + η(ε − l_k), Theorem 3 risk bound; Table I forewarning that CBF must operate at actuator-command level, not velocity setpoint, for manipulation; ε < 0 conservative regime for contact-rich tasks
- [notes/tier2/37_wang_ames_egerstedt_2017.md] — canonical CBF-QP-filter pattern (min ‖u−û‖² s.t. A_ij u ≤ b_ij); decentralized budget split αi/(αi+αj) for heterogeneous acceleration limits; hybrid braking fallback as hard safety backstop
- [notes/tier2/45_lindemann_safety_survey.md] — Table 1 differentiation scaffold establishing the Heterogeneity × Black-box × Safety × Manipulation intersection is open; §7.3 CBF-QP infeasibility under actuator limits as the exact feasibility challenge §6.2 must solve; distributed CBF constraint decomposition architecture for per-agent QP
- [notes/tier2/44_morton_2025.md] — OSCBF two-level QP (joint-space + operational-space objectives) for §6.2 within-robot filter; torque-controlled second-order formulation with full dynamics; slack + penalty relaxation for conflicting constraints; sphere-decomposition geometry primitives
- [notes/registry/stolen_ideas.md rows 37, 42, 44, 45] — CBF-QP filter, conformal slack, OSCBF QP pattern, and Table 1 scaffold all in registry with §6.2 adoption notes

The conformal update rule λ_{k+1} = λ_k + η(ε − l_k) (Huriot & Sibai 2025 §IV.A; Theorem 3) is driven by the **prediction-gap loss** l_k = max(0, predicted_h − actual_h), evaluated by the partner-trajectory predictor against the realised partner state — not by the per-step constraint-violation signal max(0, −h_ij) that the exp CBF-QP backbone produces. Theorem 3's distribution-free ε + o(1) average-loss bound is stated against the prediction gap; driving the update from −h would conflate predictor error with per-step CBF feasibility and break the cited guarantee. The implementation reflects this separation: `concerto.safety.cbf_qp.ExpCBFQP` populates `FilterInfo["constraint_violation"]` only, and `concerto.safety.conformal.update_lambda_from_predictor` (with the M3 constant-velocity predictor stub) computes and applies the prediction-gap loss, populating `FilterInfo["prediction_gap_loss"]`. The ADR-014 three-table report carries the two signals in distinct cells so per-step violations and predictor error are visible side-by-side.

**Predicted-acceleration units (2026-05-16; external-review P0-1).** The EGO_ONLY filter routes the partner's motion onto the constraint RHS as a Cartesian acceleration drift term `n_hat^T ddot p_partner_pred`. The partner-predicted acceleration is the *finite-difference* of velocity between the predictor's forecast at step `k+1` and the current snapshot at step `k`, divided by the predictor's lookahead horizon `dt`: `a_partner = (v_pred - v_now) / dt`. The `dt` parameter is a required keyword argument on `EgoOnlySafetyFilter.filter` and on the internal `concerto.safety.cbf_qp._predicted_cartesian_accel` helper; callers MUST pass the same `dt` they used when calling the partner-trajectory predictor (e.g. `concerto.safety.conformal.constant_velocity_predict`). Under the Phase-0 constant-velocity stub the velocity delta is identically zero so the corrected formula and the pre-fix raw-delta form agree at zero; the unit error was latent. A Phase-1 predictor (AoI-conditioned, learned) supplies a nonzero forecast, and any caller that previously omitted the `1/dt` scaling would have the CBF RHS wrong by a factor of `1/dt`. The scaling law is pinned by `tests/property/test_predicted_acceleration_scales_with_dt.py`.

## Mode and per-agent control model

The filter's variable layout and partner role are made explicit by a `SafetyMode` enum on `concerto.safety.cbf_qp.ExpCBFQP` and a per-uid `AgentControlModel` Protocol on `concerto.safety.api`. The full taxonomy lives in [spike_004A_safety_mode.md](spike_004A_safety_mode.md); the summary here is the contract this ADR commits to.

- **`SafetyMode.EGO_ONLY` — deployment / ad-hoc / black-box-partner default.** The QP decides only the ego agent's action; the partner's motion enters as a predicted disturbance on the constraint right-hand side. This is the mode `concerto.safety.cbf_qp.ExpCBFQP` ships in by default and the mode every ADR-009 partner-zoo evaluation must use.
- **`SafetyMode.CENTRALIZED` — oracle / ablation only.** The QP decides every uid's action; per-uid slot widths come from `control_models[uid].action_dim` with no "first uid's shape" fallback. Used as the upper-bound baseline in the ADR-014 three-table report; never used at evaluation against ad-hoc partners.
- **`SafetyMode.SHARED_CONTROL` — lab-only baseline.** Same shape as `CENTRALIZED` with an additional `partner_action_bound` that restricts the partner's adjustment magnitude. Measures how much of the centralised headroom comes from co-control budget vs from partner co-operation.

Mode is constructor-scoped, not per-call: `SafetyState.lambda_` is sized per pair, and the pair set differs between `EGO_ONLY` (one row per partner) and `CENTRALIZED` (full pairwise table). Flipping mode mid-episode would silently invalidate the conformal state; a mode change requires constructing a new filter with a fresh `SafetyState`. This is the first of two design judgment calls recorded for the §Risks #4 mitigation below.

The `AgentControlModel` Protocol mediates between each agent's action space and the shared Cartesian / safety space. The CBF row is built in Cartesian acceleration variables (`n_hat^T (a_i - a_j)`) and projected to action-space coefficients via each uid's Jacobian. For a `DoubleIntegratorControlModel` (action == Cartesian acceleration) the Jacobian is the identity and the row coefficients are byte-for-byte identical to the pre-refactor backbone; this is the migration shim every Phase-0 homogeneous test uses. For a `JacobianControlModel` (Stage-1 AS spike deliverable) the Jacobian is supplied via a callable; the placeholder raises `NotImplementedError` so 7-DOF uids fail loudly rather than silently corrupting an action slot — the same loud-fail discipline `JacobianEmergencyController` already established at the fallback boundary.

The OSCBF inner filter is *not* parameterised by `AgentControlModel`: OSCBF operates in joint space and consumes its own Jacobian (Morton & Pavone 2025 §IV). The control-model interface lives at the *inter-agent* boundary; OSCBF lives at the *per-arm* boundary. This is the second design judgment call: the `tests/property/test_oscbf.py` invariants are explicitly preserved through the refactor without being routed through `DoubleIntegratorControlModel`, because the routing would be a no-op (the migration plan in [spike_004A §Migration plan](spike_004A_safety_mode.md#-migration-plan) records this).

## Public API

The public Protocol surface for the integrated filter is split by mode so static checkers can resolve the shape of `filter()` from the constructor's mode (reviewer P0-2; spike_004A §Three-mode taxonomy). The pre-refactor single `SafetyFilter` Protocol — a dict-in / dict-out contract — did not type-check against the deployment-default `SafetyMode.EGO_ONLY` implementation, which takes a single ego `FloatArray` plus `ego_uid` and `partner_predicted_states`. Two Protocols replace it in `concerto.safety.api`:

- `EgoOnlySafetyFilter` — `filter(proposed_action: FloatArray, …, *, ego_uid, partner_predicted_states) -> tuple[FloatArray, FilterInfo]`. The deployment / ad-hoc / black-box-partner Protocol.
- `JointSafetyFilter` — `filter(proposed_action: dict[str, FloatArray], …, *, ego_uid=None, partner_action_bound=None) -> tuple[dict[str, FloatArray], FilterInfo]`. Covers `SafetyMode.CENTRALIZED` (oracle baseline) and `SafetyMode.SHARED_CONTROL` (lab-only co-control).

Both Protocols are `@runtime_checkable`. The matching typed classmethod constructors on `concerto.safety.cbf_qp.ExpCBFQP` — `ego_only`, `centralized`, `shared_control` — return values whose static type narrows to the corresponding Protocol; the low-level `ExpCBFQP(mode=…, control_models=…)` constructor remains as the documented escape hatch for tooling and tests that exercise the constructor's preconditions directly. The `filter()` method carries `typing.overload` declarations matching the two Protocol shapes so pyright resolves each constructor's return type correctly.

The pre-refactor `SafetyFilter` name is preserved as a deprecated type alias resolving to `Union[EgoOnlySafetyFilter, JointSafetyFilter]`; the first access emits a single `DeprecationWarning` via the api module's `__getattr__`. Removal target: 0.3.0. This preserves source-compatibility for external code while making the per-mode shape explicit in any new callsite. The mode-is-constructor-scoped judgment call from §"Mode and per-agent control model" still applies: a mode change requires a new filter instance with a fresh `SafetyState`, and the Protocol split now makes the invariant statically visible at the constructor's return-type annotation rather than only at filter-time.

## Consequences
- Project scope: B0 safety layer is a two-tier filter — outer tier: exp CBF-QP with conformal λ adaptation handling inter-robot collision constraints where the partner trajectory is predicted by a black-box predictor; inner tier: OSCBF QP handling within-robot task-consistency constraints at actuator command level. HO-CBF and MPC are explicitly out of Phase-2 scope.
- v0.2 plan sections affected: §6 safety architecture (conformal constraint eq. 5, update rule, ε < 0 for manipulation); §6.2 manipulator-grade filter (OSCBF two-level QP, actuator-command-level interception per Table I forewarning, per-pair budget split αi/(αi+αj)); §13 baseline design must include B_safety = pred/noLearn (λ=η=0, black-box predictor) as the safety-ablation baseline.
- Other ADRs: depends on ADR-006 (partner-policy assumption set must bound prediction error E_v and E_d to satisfy Huriot & Sibai Assumption 2); depends on ADR-007 (heterogeneity axes determine per-agent acceleration limits αi for budget split in §6.2). **Precondition for ADR-007 rev 3 Stage 3 SA spike**: the exp CBF + conformal overlay + OSCBF stack defined here must be functional (running on the Stage-1-validated rig with at least one contact-rich task) before Stage 3 SA begins; the SA spike measures the ≥20pp gap by varying per-vendor force-limit / SIL-PL pairs against this filter, so the filter cannot itself be the variable. **If ADR-007 Open Q #4 resolves to "decompose"** (safety axis splits into ISO/TS 15066-style force-limit compliance vs ISO 10218-2:2025-style functional-safety SIL/PL ratings), this ADR's filter must be extended with a per-vendor force-limit / SIL-PL handler — currently the formulation assumes a single global force threshold per pair, and ISO/TS 15066's absorption into ISO 10218-2:2025 makes per-vendor compliance variance a binding-standard concern; flagged as Open question #5 below.
- Compute / time / hiring: QP solve at kilohertz feasible on GPU; λ update is O(1) per control step; no additional hiring required in Phase 2 beyond safety engineer already planned for §6.

## Risks and mitigations
- [Theorem 3 is an average-loss guarantee, not per-step collision avoidance] In contact-rich manipulation a single step of constraint violation can cause irreversible physical damage; the conformal bound does not prevent such a spike. → Mitigation: set ε < 0 (conservative regime, per note 42 §Implications) to bias λ toward tighter constraints during contact phases; add the Wang et al. hybrid braking fallback (eq. 17 in note 37) as a hard safety backstop that bypasses the conformal QP when a braking-distance threshold is breached. The per-step backstop is embodiment-routed via the `EmergencyController` Protocol in `concerto.safety.emergency`: `maybe_brake` aggregates the per-pair Cartesian push-apart unit vectors per uid (preserving superposition when one uid is in multiple simultaneous dangerous pairs) and then dispatches each uid's aggregate to a per-uid controller that owns the Cartesian-to-control mapping. The default `CartesianAccelEmergencyController` sum-and-saturates in Cartesian space and is correct only for double-integrator agents whose control dimension equals the Cartesian dimension (the Wang-Ames-Egerstedt 2017 §V toy crossing and the Phase-0 smoke tests). The Jacobian-aware override for 7-DOF manipulators is a Stage-1 deliverable flagged in ADR-007 §"Stage 1 — Foundation axes" AS spike scope (7-DOF arm vs 2-DOF differential-drive base on a shared pick-place task); the placeholder `JacobianEmergencyController` raises `NotImplementedError` so 7-DOF uids fail loudly at the fallback boundary rather than silently writing a Cartesian-shaped vector into a joint-torque slot.
- [λ stationarity assumption breaks on partner swap] The conformal update rule assumes prediction errors are stationary; a mid-episode partner replacement (§3.5 partner zoo evaluation) invalidates this, potentially leaving λ calibrated to the wrong partner and creating an unsafe transient. → Mitigation: reinitialize λ to λ_safe (the value guaranteeing QP feasibility under worst-case bounded prediction error) on any partner-identity change detected by the capability-inference module (§3.3); treat the first N_warmup steps after swap as a high-caution window with ε_warmup < ε.
- [OSCBF known-dynamics assumption at new embodiments] Morton & Pavone 2025 require the full dynamics model M(q), c(q,q̇), g(q); adding a new arm embodiment requires system identification that introduces parametric uncertainty. → Mitigation: bound identification error as an additive disturbance in the CBF constraint slack; set the minimum slack margin ≥ worst-case identification error norm, consistent with the conformal-extension framing in note 42 and the per-step safety argument.
- [Pre-refactor QP layout assumed homogeneous action_dim and co-controllable partner] The pre-refactor `concerto.safety.cbf_qp.ExpCBFQP` derived `n_u_per_agent` from the first uid's action shape and returned safe actions for every uid in a dict — a homogeneous, centralised solve. Reviewer P0-2 flagged this as inconsistent with the AS axis of ADR-007 (7-DOF arm vs 2-DOF base requires per-uid `action_dim`); reviewer P0-3 flagged the centralised solve as inconsistent with the black-box-partner contract of ADR-006 (deployment must not co-control the partner). → Mitigation: the heterogeneity-aware refactor specified in [spike_004A_safety_mode.md](spike_004A_safety_mode.md) and summarised in §"Mode and per-agent control model" above. `SafetyMode.EGO_ONLY` is the deployment default and routes the partner through the constraint RHS as a predicted disturbance; per-uid `AgentControlModel` carries the action ↔ Cartesian map so heterogeneous embodiments build correctly-shaped rows. The placeholder `JacobianControlModel` raises `NotImplementedError` until the Stage-1 AS spike supplies a kinematic model — the same loud-fail discipline that protects the fallback path. Two design judgment calls recorded: (1) mode is constructor-scoped, so flipping it mid-episode cannot silently invalidate the conformal slack vector's per-pair sizing; (2) `tests/property/test_oscbf.py` is intentionally not routed through `DoubleIntegratorControlModel` because OSCBF is per-arm joint-space and already Jacobian-parameterised — the routing would be a no-op and the spec instruction does not apply. → Additional mitigation (reviewer P0-3 follow-up): the EGO_ONLY row builder concentrates three sign-sensitive transformations in one place — the ego acceleration coefficient `-n_hat^T a_ego` on the LHS, the partner-disturbance subtraction `n_hat^T a_partner_pred` on the RHS, and the closing-velocity inner product `n_hat·Δv` inside `h_ij` — and a flipped sign in any of them would silently invert the constraint and make the entire filter unsafe without changing the QP-solver-visible behaviour in benign configurations. The integrated tests cover the homogeneous CENTRALIZED case but do not isolate the EGO_ONLY disturbance term; to close that gap the row builder is exposed as the private `concerto.safety.cbf_qp._build_ego_only_row` helper (an internal seam the production `_filter_ego_only` consumes directly) and pinned analytically against six closed-form scenarios plus a hand-rolled mutant-detector test in `tests/unit/test_cbf_qp_ego_only_signs.py`. The mutant verifies the assertions themselves catch the flipped-sign bug class; a reduction test pins the spike_004A §Reduction promise that the EGO_ONLY row collapses to the CENTRALIZED ego slot under `DoubleIntegratorControlModel` with zero partner-predicted acceleration. The sign-convention test file is the guard rail this risk entry inherits going forward.

## Reversibility
Type-2 (two-way door). The CBF formulation (order, function family) is encapsulated in the QP constraint-generation module; switching from exp CBF to HO-CBF in Phase 3 requires refactoring that module only. The conformal adaptation layer and the OSCBF QP wrapper interface are order-agnostic. No capital expenditure, irreversible infrastructure, or external commitment is gated on the Phase-2 formulation choice.

## Validation criteria
By Phase-2 end: (1) B0 with CBF-QP + conformal safety filter achieves <2% inter-robot collision rate across ≥500 evaluation episodes under standard conditions and <5% under 200ms comm latency + 30% packet drop; (2) conformal λ update reduces collision rate by ≥20% relative to the pred/noLearn ablation baseline (λ=η=0, matching Huriot & Sibai Table I structure); (3) OSCBF QP solve time <1ms per control step on Phase-2 evaluation hardware.

## Open questions deferred to a later ADR
- Can Theorem 3's average-loss bound (note 42) be sharpened to a high-probability per-step safety guarantee for manipulation, where a single constraint violation may cause irreversible physical damage? #verify — if not, the hard braking fallback (note 37) becomes load-bearing and must be validated separately.
- What is the minimum predictor warm-up data required to satisfy Assumption 2 (bounded E_v, E_d) for a black-box VLA partner (e.g., OpenVLA, note 32) whose action distribution may be multi-modal? #verify — feeds Phase-2 partner-predictor specification; resolves with ADR-006.
- Does the per-pair budget split αi/(αi+αj) extend correctly when one agent is a mobile base (velocity-controlled) and the other is a torque-controlled arm — i.e., when the CBF relative degree differs across embodiment classes? #design-decision — defer to Phase-2 CBF design document with note 37 and note 44 as evidence.
- HO-CBF (Option B, Kim 2026, Tier-3 #43) for manipulation-grade higher-DoF coupling — defer to Phase-3 ADR pending note 43 reading completion.
- (#design-decision, from ADR-007 rev 3 Open Q #4) If the safety axis decomposes into per-vendor force-limit compliance and SIL/PL ratings, must this ADR add a per-vendor force-limit handler in §6.2 — i.e., a CBF constraint that takes a vendor-tag input and selects from a force-limit table — or is it sufficient to keep one global threshold and treat per-vendor variance as a §6.3 reporting concern (ADR-014)? Resolution requires ADR-007 Stage 3 SA spike outcome and the post-spike safety-decomposition review.
- (#design-decision, **`lambda_safe` derivation deferred**) The risk-mitigation #2 partner-swap reset in `concerto.safety.conformal.reset_on_partner_swap` currently uses a Phase-0 placeholder default `lambda_safe=0.0`. The risk entry and §Rationale frame `lambda_safe` as the value guaranteeing QP feasibility under the worst-case bounded prediction error envelope from ADR-006 Assumption A2, i.e. the derived form `lambda_safe(bounds, predictor_error_bound, dt, pair_geometry)` parameterised by the four dependencies: (1) `Bounds` (action / rate / latency / force envelope per ADR-006 §Decision Option C), (2) the partner-trajectory predictor error bound (Huriot & Sibai 2025 Assumption 2; constant-velocity stub in Phase-0), (3) the control-step `dt`, and (4) per-pair geometry (relative position, velocity, and safety distance from `AgentSnapshot`). The derivation has not been carried out; the default `0.0` is honest only as a placeholder, not as a feasibility certificate. Resolution requires the per-step safety bound work flagged in the first §Open question above and the Stage-1 AS-spike evidence per §Validation criteria; until then, callers requiring provable safety MUST override `lambda_safe` explicitly. See the `reset_on_partner_swap` docstring for the per-caller override guidance.

## Revision history
- 2026-05-13 revision: adds foundational CBF and CP citations (Ames 2017, Ames 2019, Shafer-Vovk 2008, Angelopoulos-Bates 2023) in Context, Rationale, and Evidence basis. No change to the Decision. Ties to the new public literature page at docs/reference/literature.md.
- 2026-05-13 status re-classification: status changed from Proposed to **Provisional** under the new ADR status taxonomy (see [ADR-INDEX §Status taxonomy](ADR-INDEX.md#status-taxonomy)); claims remain qualified by §Open questions; no Decision content is altered.
- 2026-05-13 lock: promoted to **Accepted (2026-05-13)** under the solo-developer working policy in ADR-INDEX; the per-step-bound and safety-stack refactor remain flagged as Open work pointing into §Open questions; no Decision content is altered.
- 2026-05-13 amendment (spike_004A): adds §"Mode and per-agent control model" linking to [spike_004A_safety_mode.md](spike_004A_safety_mode.md); adds §Risks entry 4 covering the heterogeneity-aware refactor that mitigates reviewer P0-2 (homogeneous QP variable layout) and P0-3 (centralised solve at deployment). Records the two design judgment calls (constructor-scoped mode; `test_oscbf.py` left unchanged because OSCBF is per-arm joint-space). No change to the Decision; status stays **Accepted (2026-05-13)** with the per-step-bound footnote (ADR-INDEX footnote a) still pointing at the inherited §Open questions. Promotion to **Validated** still requires the Stage-1 AS spike evidence per §Validation criteria.
- 2026-05-13 amendment (reviewer P0-2 follow-up): adds §"Public API" splitting the single `SafetyFilter` Protocol into `EgoOnlySafetyFilter` and `JointSafetyFilter`, with typed `ExpCBFQP.ego_only` / `.centralized` / `.shared_control` classmethod constructors and `typing.overload` declarations on `ExpCBFQP.filter`. The pre-refactor `SafetyFilter` name remains as a deprecated union alias (`DeprecationWarning` via module `__getattr__`; removal target 0.3.0). No change to the Decision; status stays **Accepted (2026-05-13)**.
- 2026-05-13 amendment (reviewer P0-3 follow-up): extends §Risks entry 4 with a sign-convention test suite as an additional mitigation. Exposes the private `concerto.safety.cbf_qp._build_ego_only_row` helper as the internal row-builder seam that `_filter_ego_only` consumes directly; adds `tests/unit/test_cbf_qp_ego_only_signs.py` with six analytic scenarios, a hand-rolled mutant-detector that proves the assertions catch the flipped-sign bug class, and a reduction test pinning the spike_004A §Reduction promise. No change to the Decision; status stays **Accepted (2026-05-13)**.
- 2026-05-16 amendment (external-review P0-4): adds an explicit §Open question entry naming the `lambda_safe` derivation as deferred (four dependencies: `Bounds`, predictor error bound, `dt`, pair geometry). Updates the `reset_on_partner_swap` docstring and the `concerto.safety.conformal` module docstring to mark the `lambda_safe=0.0` default as a Phase-0 placeholder rather than a derived QP-feasibility-preserving value, and to direct callers requiring provable safety to override the parameter explicitly. The ADR-INDEX [footnote a](ADR-INDEX.md#open-work-flags) is extended to surface the deferral. No change to the Decision; status stays **Accepted (2026-05-13)**.
- 2026-05-16 amendment (external-review P0-1): adds the "Predicted-acceleration units" paragraph to §Decision. The partner-predicted Cartesian acceleration on the EGO_ONLY filter's constraint RHS is now `(v_pred - v_now) / dt` (was: `v_pred - v_now` — velocity delta, wrong units). The `dt` parameter is threaded through `EgoOnlySafetyFilter.filter`, `ExpCBFQP.filter`, and the internal `_predicted_cartesian_accel` helper; the scaling law is pinned by `tests/property/test_predicted_acceleration_scales_with_dt.py`. Latent under the Phase-0 constant-velocity predictor stub (zero delta ⇒ zero accel ⇒ no observable change at scale `1/dt`); becomes load-bearing the moment a Phase-1 predictor supplies a nonzero forecast. No change to the Decision; status stays **Accepted (2026-05-13)**.
