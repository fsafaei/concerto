# Tutorial: Hello-spike

!!! note "Phase-0 placeholder"
    This tutorial will be filled once the Stage-0 smoke spike (M1) is
    implemented. It walks through running a single-axis spike end-to-end.

## What you will build

A minimal Stage-0 smoke run against the CHAMBER-Solo precursor task.

## Prerequisites

- CHAMBER installed (`uv sync --group dev`)
- A GPU with CUDA 12.1+ (or the CPU fallback for the smoke task)

## Steps

1. Verify the pre-registration SHA matches the tagged file.
2. Launch the smoke spike.
3. Inspect the result archive.

*(Full content added in M1.)*
