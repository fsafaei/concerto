# SPDX-License-Identifier: Apache-2.0
"""CHAMBER console entry points — chamber-spike, chamber-eval, chamber-render-tables.

CLI modules are thin wrappers over ``chamber.benchmarks``, ``chamber.evaluation``,
and ``chamber.evaluation.reports``. Entry points are declared in
``pyproject.toml`` ``[project.scripts]``.
"""
